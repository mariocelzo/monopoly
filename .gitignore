# Dipendenze installate: si rigenerano con npm install
node_modules/

# Output di build di Vite
dist/

# Cache incrementale di TypeScript
*.tsbuildinfo

# Variabili d'ambiente locali (VITE_SERVER_URL, CLIENT_ORIGIN, ...)
.env
.env.local

# File di sistema macOS
.DS_Store

# Log di npm
npm-debug.log*
.superpowers/

# Partite salvate da PERSIST_FILE: sono stato di gioco, non codice, e
# contengono i nomi dei giocatori. Il percorso suggerito in .env.example è
# dentro il repo, quindi senza questa riga finirebbero committate.
data/
*.rooms-state.json
rooms-state.json
